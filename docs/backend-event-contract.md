# Backend event contract — moved

All backend **requirements**, request/response contracts, and field mappings now live in a single file:

**[backend-requirements.md](./backend-requirements.md)**

This stub remains so older links to `backend-event-contract.md` keep working.
