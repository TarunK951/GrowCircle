/** Dispatched when the regional metro `<select>`-style picker opens/closes (see `RegionalMeetsSection`). */
export const REGIONAL_METRO_PICKER_EVENT = "gc-regional-metro-picker";

export type RegionalMetroPickerDetail = { open: boolean };
